A dynamic File Explorer UI built with React that mimics a folder structure.
This project demonstrates recursive component rendering, tree data manipulation, and dynamic add/delete operations within nested folders.

 Features:

Recursive folder & file rendering

Expand / collapse folders

Add new folders dynamically

Delete folders/files

Infinite nested structure support

Tree data update using recursion

Clean component separation
